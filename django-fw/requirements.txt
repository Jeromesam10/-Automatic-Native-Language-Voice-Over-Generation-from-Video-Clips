asgiref==3.11.1
django>=5.2,<6
gunicorn==23.0.0
python-dotenv==1.1.1
sqlparse==0.5.5

# Local TTS (MMS-TTS, Coqui XTTS, FreeVC)
# Coqui TTS 0.22 supports Python >=3.9,<3.12, so use Python 3.11 for voice cloning.
numpy>=1.24.0,<2
soundfile>=0.12.1
# torch <2.6 keeps the legacy torchaudio loaders (no torchcodec needed) and the
# pre-2.6 torch.load behavior that Coqui TTS 0.22.0 relies on.
torch>=2.1.0,<2.6
torchaudio>=2.1.0,<2.6
transformers>=4.36.0,<4.41
langdetect>=1.0.9
TTS==0.22.0
